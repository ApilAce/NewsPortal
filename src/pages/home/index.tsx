import React from "react";
import Head from "next/head";

const Homepage = () => {
  return (
    <div>
      <Head>
        <title>Homepage</title>
        <meta name="description" content="Homepage" />
      </Head>
      <div>Homepage</div>
    </div>
  );
};

export default Homepage;

// .container

// Home.module.css ---> .container ---> global variable
